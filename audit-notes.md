# Site audit

- Tighten hero top spacing at every breakpoint so the fixed header does not leave a large visual void.
- Keep the mobile experience performant by reducing the fixed procedural grain overlay on small screens.
- Make the canonical/site URL match the live Vercel domain used by the deployment.
- Add metadata hardening for mobile browsers and search/social sharing without changing the existing content architecture.
- Preserve the existing editorial visual language, CMS-driven content model, and responsive component structure.
