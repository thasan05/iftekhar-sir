/**
 * One easing curve for the whole page, so the hero entrance and the scroll
 * reveals feel like the same hand. Deliberately non-overshooting.
 */
export const EASE_MANUSCRIPT: [number, number, number, number] = [0.22, 1, 0.36, 1];
